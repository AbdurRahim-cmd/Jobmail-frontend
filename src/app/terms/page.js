import TermsOfService from "@/components/legal/TermsOfService";

export const metadata = {
  title: "Terms of Service — JobMailer",
  description:
    "The rules for using JobMailer, your responsibilities when sending recruiter emails, and the limits of our liability.",
};

export default function TermsPage() {
  return <TermsOfService />;
}
